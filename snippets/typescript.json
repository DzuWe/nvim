{
  "Define Vue Props": {
    "prefix": "dzPropsTs",
    "body": [
      "interface Props {$0}",
      "const props = defineProps<Props>()"
    ],
    "description": "Define props"
  },
  "Define Vue Short Props": {
    "prefix": "dzPropsTsShort",
    "body": [
      "defineProps<$0>()"
    ],
    "description": "Define props"
  },
	"Create Vue Provider": {
		"prefix": "dzVueProvider",
		"body": [
			"import { _throw } from 'offering-ui/utils'",
			"import { inject, provide } from 'vue'",
			"import type { InjectionKey } from 'vue'",
			"",
			"export type $1Provider = ReturnType<typeof provide${2:$1}>",
			"const INJECTION_KEY: InjectionKey<${2:$1}Provider> = Symbol('${3:$1}')",
			"",
			"export function provide${4:$1}() {",
			"    const composable = {}",
			"    provide(INJECTION_KEY, composable)",
			"    return composable",
			"}",
			"",
			"export const inject${5:$1} = () => {",
			"    return inject(INJECTION_KEY) || _throw('[Error] [${6:$1} provider] ${7:$1} provider is undefined')",
			"}"
		],
		"description": "Define base provider"
	},
	"Add return type": {
		"prefix": "dzrt",
		"body": [
			"ReturnType<typeof $0>"
		],
		"description": "shortcut to ReturnType"
	}
}
